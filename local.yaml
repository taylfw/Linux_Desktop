---
- hosts: localhost
  connection: local
  become: true
  tasks:
    - name: Install Applications
      package:
        name:
          - htop
          - tmux
          - vim
          - curl
          - nmap
          - cmatrix
          - calibre
          - vlc
          - gimp

